server:
  port: 8080
spring:
  application:
    name: users
  datasource:
    password: password
    url: jdbc:mysql://localhost:3308/users
    username: damian
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true